import { responseType } from '@/components/utils/SanityProductsTypes'
import React, { FC } from 'react'

const ProductCarousel:FC<{res:Array<responseType>} > = ({res}) => {
  return (
    <div>
     
    </div>
  )
}

export default ProductCarousel